<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.SocialMemberMapper">

    <!-- 소셜 회원 등록 -->
    <insert id="insert" parameterType="com.app.springapp.domain.vo.SocialMemberVO">
        INSERT INTO TBL_SOCIAL_MEMBER (
            ID,
            SOCIAL_MEMBER_PROVIDER_ID,
            SOCIAL_MEMBER_PROVIDER,
            MEMBER_ID
        ) VALUES (
            SEQ_SOCIAL_MEMBER.NEXTVAL,
            #{socialMemberProviderId},
            #{socialMemberProvider},
            #{memberId}
        )
    </insert>

</mapper>
